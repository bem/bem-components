block('checkbox').elem('label')(

    tag()('label'),

    attrs()(function() {
        var attrs = this.ctx.attrs || {};
        attrs['for'] = this._checkboxAttrs.id;
        return attrs;
    })

)
