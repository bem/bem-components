block('cut')(
    js()(true),

    content()(function() {
        return [
            { elem : 'container', content : applyNext() },
            { elem : 'switcher', content : this.ctx.switcher || 'Show' }
        ];
    })
);
