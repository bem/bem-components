block('cut')(

    js()(true),

    content()(function() {
        return [
            { elem : 'container', content : applyNext() },
            {
                block : 'link',
                mods : { type : 'pseudo', theme : 'islands' },
                mix : { block : 'cut', elem : 'switcher' },
                content : this.ctx.switcher || 'Show' }
        ];
    })

);
