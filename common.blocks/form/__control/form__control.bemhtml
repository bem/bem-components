block('form').elem('control')(

    js()(true),

    cls()('i-bem'),

    tag()('input'),

    attrs()(function() {
        var ctx = this.ctx;
        return {
            id: ctx.id,
            name: ctx.name,
            value: ctx.val
        };
    })

);
