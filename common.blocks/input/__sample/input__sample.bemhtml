block('input').elem('sample').tag()('span')

block('b-link')
    .match(!this.elem)
    .match(this._input && this._input.mods && this._input.mods.disabled === 'yes')
    .match(function() { return !this.mods.disabled })
    .def()(function() {
            apply(
                '',
                { 'ctx.mods': this._.extend(this.ctx.mods, { disabled : 'yes' }) }
            )
        })
