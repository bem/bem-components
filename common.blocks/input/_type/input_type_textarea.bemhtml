block('input').mod('type', 'textarea').elem('control')(

    tag()('textarea'),

    attrs()(function() {
        var attrs = applyNext();
        delete attrs.value;
        return this._.extend({ rows: 10, cols: 10 }, attrs);
    })

    content()(function() { return this._input.value })

)
