block('radio-option').elem('control')(
    tag()('input'),
    attrs()(function() {
        var res = { type : 'radio' },
            ctx = this.ctx;

        res.value = ctx.val;
        res.name = ctx.name;
        ctx.checked && (res.checked = 'checked');
        ctx.disabled && (res.disabled = 'disabled');

        return res;
    })
)
