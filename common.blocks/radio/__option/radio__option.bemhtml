block('radio').elem('option')(
    tag()('label'),
    js()(true),
    mix()({ block: 'i-bem' }),
    content()(function() {
        var ctx = this.ctx;
        return [
            {
                elem : 'box',
                content : {
                    elem : 'control',
                    checked : this.elemMods.checked,
                    disabled : this.elemMods.disabled,
                    name : ctx.name,
                    val : ctx.val
                }
            },
            ctx.text
        ];
    })
)