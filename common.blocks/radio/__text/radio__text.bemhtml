block('radio').elem('text')(
    tag()('span'),
    attrs()(function() {
        return {
            role : 'presentation',
            'aria-hidden' : true /* NOTE: JAWS processes role="presentation" wrongly */
        };
    })
);
