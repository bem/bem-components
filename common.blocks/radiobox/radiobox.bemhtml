block radiobox {

    tag: 'span'

    js: true

    default, !this._name: {
        applyNext(
            this._mods = this.ctx.mods,
            this._name = this.ctx.name,
            this._value = this.ctx.value
        );
    }

}