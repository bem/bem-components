block('rating').mod('readonly', true) (
    content()(function() {
        var ctx = this.ctx,
            mods = this.mods,
            _sizes = {
                s : 12,
                m : 16
            };

        return [
            {
                elem : 'label',
                attrs : { style : 'width: ' + ctx.granulation * _sizes[mods.size] + 'px;' }
            },
            {
                elem : 'result',
                attrs : { style : 'width: ' + (ctx.points || 0) * _sizes[mods.size] + 'px;' }
            }
        ];
    })
);
