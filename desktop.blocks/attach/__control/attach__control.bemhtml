block attach, elem control {

    tag: 'input'

    attrs: {
        var a = {
                value: '',
                type: 'file',
                id: this._id,
                name: this._name
            };

        !!this._disabled && (a.disabled = 'disabled');
        this._tabindex && (a.tabindex = this._tabindex);

        return a;
    }

}
