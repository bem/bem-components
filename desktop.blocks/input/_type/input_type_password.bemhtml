block input, mod type password {

    elem input {

        attrs: {
            // FIXME: gh/bem-bl/issues/284
            var a = applyNext();
            return this._.extend(a, { 'type': 'password' });
        }

    }

}
